const { expect } = require("chai");
const { ethers } = require("hardhat");
const BigNumber = ethers.BigNumber;

describe("#Testing Comp Contract", () => {
    it("comp contract should deploy properly",()=>{

    })
    
})